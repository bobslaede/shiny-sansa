db:
  image: dockerfile/mongodb
  ports:
    - 27017

web:
  build: .
  volumes:
    - .:/app
  working_dir: ./app
  ports:
    - "3007:3007"
  command: iojs --harmony_arrow_functions index.js
  links:
    - db
  environment:
    PORT: 3007
    DB: 27017
